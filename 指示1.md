# 実装依頼
https://github.com/smorce/agent-router-opik-lab

既存プロジェクトに、以下のLLMアクセス基盤を実装してください。

最終的な目標は、アプリケーションから特定のLLMサーバーを直接呼ばず、Agent Routerを共通のLLM Gatewayとして利用できるようにすることです。

今回はTask Routerの本物の推論ロジックは実装しません。

Task Routerは交換可能なダミー実装にしてください。

既存のローカルLLMサーバーはすでに起動済みです。

---

# 1. 今回構築する構成

以下の構成にしてください。

```text
Application
    │
    ▼
Task Router
Dummy実装
    │
    ▼
Agent Router
http://127.0.0.1:1975/v1
    │
    ▼
既存 llama-server
http://127.0.0.1:2067/v1
    │
    ▼
qwen3.8-27b-exl3-3.5bpw-wm


Agent Router
    │
    │ OpenTelemetry
    ▼
Opik
http://127.0.0.1:5173
    │
    ├─ Trace
    ├─ Token usage
    ├─ Latency
    ├─ Cost
    └─ Evaluation
```

将来的には以下のように拡張する予定です。

```text
                         ┌─ OpenAI
                         ├─ Anthropic
Application              ├─ Gemini
    │                    ├─ Bedrock
    ▼                    ├─ Groq
Task Router              └─ Local LLM
    │                       ▲
    │                       │
    └──────► Agent Router ──┘
                  │
                  ▼
                Opik
```

今回の実装で、この将来構成を妨げる設計にしないでください。

---

# 2. 最重要方針

以下を厳守してください。

1. Applicationからllama-serverを直接呼ばない
2. 通常の推論は必ずAgent Routerを経由する
3. Task Routerは独立したインターフェースにする
4. 今回のTask Router実装はDummyTaskRouterとする
5. 将来、分類モデルやLLM Routerへ差し替え可能にする
6. Agent RouterはProvider通信とRoutingの責任を持つ
7. Opikは観測・評価の責任を持つ
8. APIキーやURLをソースコードへ直書きしない
9. 既存の環境変数によるLLM設定思想は維持する
10. 既存コードを可能な限り壊さない
11. 実装前にリポジトリ全体を確認する
12. すでに同等の仕組みが存在する場合は重複実装しない
13. 不要な巨大フレームワークを導入しない
14. Kubernetesは今回は導入しない
15. LiteLLMは導入しない

今回の目的は、まずローカル環境で次の経路を確実に完成させることです。

```text
Application
→ DummyTaskRouter
→ Agent Router
→ llama-server
→ response

Agent Router
→ OpenTelemetry
→ Opik
```

---

# 3. 現在のLLMサーバー

すでに以下でOpenAI互換llama-serverが動作しています。

```text
LLAMA_SERVER_BASE_URL=http://127.0.0.1:2067
LLAMA_SERVER_API_KEY=sk-local-no-key-required
LLM_MODEL=qwen3.8-27b-exl3-3.5bpw-wm
```

OpenAI互換APIは、

```text
http://127.0.0.1:2067/v1
```

です。

既存実装ではOpenAI Python SDKを使っています。

現在の主な設定値は以下です。

```text
LLAMA_SERVER_ENABLE_THINKING
LLAMA_SERVER_TEMPERATURE
LLAMA_SERVER_TOP_P
LLAMA_SERVER_TOP_K
LLAMA_SERVER_MIN_P
LLAMA_SERVER_MAX_TOKENS
LLAMA_SERVER_TIMEOUT_SECONDS
LLAMA_SERVER_MAX_RETRIES
LLAMA_SERVER_RETRY_BASE_SECONDS
LLAMA_SERVER_RETRY_MAX_SECONDS
LLM_MODEL
```

これらの設定思想を可能な限り維持してください。

---

# 4. Agent Router

Agent Router 1.1系を使用してください。

旧名称はEnvoy AI Gatewayですが、現在の名称であるAgent Routerを使用してください。

CLI名は現在も、

```text
aigw
```

です。

開発環境ではStandaloneモードを使用します。

```text
aigw run
```

を利用し、

```text
http://127.0.0.1:1975/v1
```

をアプリケーションから見える唯一のLLM Gatewayにしてください。

---

# 5. llama-serverをAgent Routerへ接続

最初はAgent Routerの環境変数による自動設定を優先してください。

概念的には以下です。

```bash
OPENAI_BASE_URL=http://127.0.0.1:2067/v1
OPENAI_API_KEY=sk-local-no-key-required
aigw run
```

ただし、実装時には現在のAgent Router 1.1公式仕様を必ず確認してください。

古いEnvoy AI Gatewayの記事や古い0.x系ドキュメントを盲目的に使わないでください。

Agent Routerの起動後、

```text
http://127.0.0.1:1975/v1
```

をOpenAI互換APIとして使用します。

---

# 6. Task Router

将来のためにTask Routerを独立させてください。

最低限、以下に相当する構造を作ってください。

```python
from dataclasses import dataclass
from typing import Protocol


@dataclass(frozen=True)
class RouteDecision:
    model: str
    provider: str
    reason: str


class TaskRouter(Protocol):
    async def route(
        self,
        *,
        requested_model: str,
        prompt: str,
    ) -> RouteDecision:
        ...


class DummyTaskRouter:
    async def route(
        self,
        *,
        requested_model: str,
        prompt: str,
    ) -> RouteDecision:
        ...
```

厳密なファイル名や引数は既存コードに合わせて変更して構いません。

重要なのは責任分離です。

---

# 7. DummyTaskRouterの動作

初期状態では、

```text
model="Auto"
```

の場合、

```text
qwen3.8-27b-exl3-3.5bpw-wm
```

へ固定で振り分けてください。

例:

```text
requested_model = Auto

↓

DummyTaskRouter

↓

RouteDecision(
    model="qwen3.8-27b-exl3-3.5bpw-wm",
    provider="local",
    reason="dummy_default_route"
)
```

ただし、

```text
model="明示的なモデル名"
```

が指定された場合の扱いは拡張可能にしてください。

基本方針としては、明示的なモデル指定をそのままAgent Routerへ渡せる設計を優先してください。

DummyTaskRouter自身がHTTP通信をしてはいけません。

DummyTaskRouterの仕事は、

```text
どのmodelを要求するか
```

を決めるところまでです。

---

# 8. 将来のTask Router差し替え

将来的には、

```text
DummyTaskRouter
```

を、

```text
ClassifierTaskRouter
LLMTaskRouter
MoETaskRouter
```

などへ置き換えます。

将来は例えば、

```text
入力タスク
 ↓
難易度判定
 ↓
タスク種別判定
 ↓
コスト制約
 ↓
モデル選択
```

を行います。

したがってTask RouterとAgent Routerを密結合させないでください。

責任は次のように分離します。

```text
Task Router
= どのモデルを使うか判断

Agent Router
= そのモデルへの通信、Provider差分、負荷分散、
  Retry、Fallback、Rate Limitなど

Opik
= Trace、評価、分析
```

---

# 9. Application側LLM Client

Applicationからは、なるべく以下のような簡単なAPIで呼べるようにしてください。

```python
client = LLMClient.from_env()

text = await client.complete(
    "Hello",
    model="Auto",
)
```

または既存APIとの互換性を維持する必要がある場合、

```python
cfg = LlamaServerEnvConfig.from_env()
text = await cfg.complete("Hello")
```

も壊さないようにしてください。

ただし内部では、

```text
Application
→ Task Router
→ Agent Router
```

を通してください。

新しい主要クラス名は、

```text
LLMGatewayClient
LLMGatewayEnvConfig
```

など、llama-server以外のProviderでも意味が通る名前を推奨します。

既存の、

```text
LlamaServerEnvConfig
```

を外部から利用している場合は、

* compatibility wrapper
* alias
* deprecated wrapper

などを使って互換性を維持してください。

いきなり削除しないでください。

---

# 10. OpenAI Clientの接続先

Application側のOpenAIクライアントは、原則として以下を向くように変更してください。

変更前:

```text
http://127.0.0.1:2067/v1
```

変更後:

```text
http://127.0.0.1:1975/v1
```

つまり、

```python
OpenAI(
    base_url="http://127.0.0.1:1975/v1",
    ...
)
```

相当になります。

llama-serverのURLをApplication側の通常推論経路から直接参照しないでください。

llama-serverのURLを知るのはAgent Router起動設定側です。

---

# 11. Chat Completionsを初期標準経路にする

現在の既存コードには、

```python
client.responses.create(...)
```

があります。

Agent Router 1.1はResponses APIに対応していますが、今回の初期実装では、観測性の確認を優先して、

```text
POST /v1/chat/completions
```

を標準経路にしてください。

つまりPythonでは、

```python
client.chat.completions.create(...)
```

を第一候補にしてください。

理由は、Agent RouterのOpenTelemetryトレースについて、Chat Completionsが明確にサポート対象として文書化されているためです。

ただしResponses APIを削除する必要はありません。

将来、

```text
LLM_API_STYLE=chat_completions
LLM_API_STYLE=responses
```

のように切り替え可能にしても構いません。

初期値は、

```text
chat_completions
```

にしてください。

---

# 12. llama.cpp固有パラメータ

現在利用している、

```text
top_k
min_p
chat_template_kwargs
enable_thinking
```

はOpenAI標準APIだけのパラメータではありません。

既存コードでは、

```python
extra_body={
    "top_k": ...,
    "min_p": ...,
    "chat_template_kwargs": {
        "enable_thinking": ...
    },
}
```

として送信しています。

この機能を可能な限り維持してください。

ただし、ここは必ずEnd-to-Endで検証してください。

検証経路:

```text
Python OpenAI SDK
↓
Agent Router
↓
llama-server
```

確認対象:

```text
temperature
top_p
top_k
min_p
chat_template_kwargs.enable_thinking
max_tokens
model
```

Agent Routerが追加フィールドを削除、拒否、変換していないか確認してください。

動作確認せずに「通るはず」と判断してはいけません。

もし、

```text
top_k
min_p
chat_template_kwargs
```

のいずれかがAgent Router経由では維持できない場合、

1. 何が失われるか
2. Agent Routerのどこで失われるか
3. llama-serverへ実際に届いている内容
4. 回避方法があるか

を調査し、報告してください。

その場合も、黙ってApplicationからllama-serverへの直接接続へ戻してはいけません。

---

# 13. Thinking設定

既存仕様を維持してください。

Thinking有効時の既定値:

```text
temperature = 1.0
top_p = 0.95
top_k = 20
min_p = 0.0

chat_template_kwargs = {
    "enable_thinking": True
}
```

Thinking無効時:

```text
temperature = 0.7
top_p = 0.8
top_k = 20
min_p = 0.0

chat_template_kwargs = {
    "enable_thinking": False
}
```

環境変数に値が指定された場合は、既存仕様と同じく明示値を優先してください。

---

# 14. Retry責任の整理

Retryを複数層で無制限に重ねないでください。

以下のような状態を避けます。

```text
Application 5 retries
    ×
Agent Router 5 retries
    ×
Provider 5 retries
```

これは最悪25回以上の推論につながります。

今回の単一バックエンド環境では、

Application側:

```text
Agent Routerそのものへの接続エラー
一時的な5xx
タイムアウト
```

だけを対象とした少数回のRetryにしてください。

OpenAI SDK自身の自動Retryは、

```text
max_retries=0
```

を基本とします。

将来Provider FallbackやBackend RetryをAgent Routerへ実装した場合は、ProviderレベルのRetry責任をAgent Router側へ移せる構造にしてください。

---

# 15. 環境変数

`.env.example`を整備してください。

最低限、以下を設定可能にしてください。

```dotenv
# Application → Agent Router
AGENT_ROUTER_BASE_URL=http://127.0.0.1:1975
AGENT_ROUTER_API_KEY=unused

# Task Router
TASK_ROUTER_MODE=dummy
TASK_ROUTER_DEFAULT_MODEL=qwen3.8-27b-exl3-3.5bpw-wm

# Agent Router → llama-server
LLAMA_SERVER_BASE_URL=http://127.0.0.1:2067
LLAMA_SERVER_API_KEY=sk-local-no-key-required

# Model
LLM_MODEL=qwen3.8-27b-exl3-3.5bpw-wm
LLM_DEFAULT_REQUEST_MODEL=Auto

# API style
LLM_API_STYLE=chat_completions

# Generation
LLAMA_SERVER_ENABLE_THINKING=false
LLAMA_SERVER_TEMPERATURE=0.3
LLAMA_SERVER_TOP_P=
LLAMA_SERVER_TOP_K=
LLAMA_SERVER_MIN_P=
LLAMA_SERVER_MAX_TOKENS=1000

# Client reliability
LLAMA_SERVER_TIMEOUT_SECONDS=15
LLAMA_SERVER_MAX_RETRIES=3
LLAMA_SERVER_RETRY_BASE_SECONDS=0.8
LLAMA_SERVER_RETRY_MAX_SECONDS=8.0

# Opik
OPIK_ENABLED=true
OPIK_BASE_URL=http://127.0.0.1:5173
OPIK_PROJECT_NAME=agent-router-local

# OpenTelemetry
OTEL_TRACES_EXPORTER=otlp
OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://127.0.0.1:5173/api/v1/private/otel/v1/traces

# Agent Router tracing
AI_GATEWAY_TRACING_SEMCONV=openinference

# Development only:
OPENINFERENCE_HIDE_INPUTS=false
OPENINFERENCE_HIDE_OUTPUTS=false
```

実際のAgent Router / Opikの現行仕様に合わせ、必要なら変数名を調整してください。

`.env.example`には秘密情報を書かないでください。

---

# 16. OpenTelemetry → Opik

Agent RouterからOpikへOpenTelemetry Traceを送ってください。

OpikのSelf-hosted OpenTelemetry trace endpointは、現行公式仕様を確認したうえで使用してください。

想定は、

```text
http://127.0.0.1:5173/api/v1/private/otel/v1/traces
```

です。

OpikはOpenTelemetryについてHTTP transportを使用するため、

```text
http/protobuf
```

を利用してください。

gRPC前提にしないでください。

Agent Router側では、

```text
OTEL_TRACES_EXPORTER=otlp
OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=...
```

などを適切に設定してください。

---

# 17. OpenInference

初期の開発・評価用途では、

```text
AI_GATEWAY_TRACING_SEMCONV=openinference
```

を第一候補としてください。

これによりLLMの入力・出力を含む詳細なTraceをOpik側で評価しやすくします。

ただし、プロンプトや回答内容は機密情報になる可能性があります。

そのため、

```text
OPENINFERENCE_HIDE_INPUTS
OPENINFERENCE_HIDE_OUTPUTS
```

を必ず環境変数で変更可能にしてください。

READMEにも、

```text
本番環境で入力・出力を記録する場合は、
個人情報・機密情報をOpikへ保存してよいか確認すること
```

という注意を書いてください。

---

# 18. Opik

Opikはセルフホストを想定します。

すでにOpikが存在する場合は再インストールしないでください。

存在しない場合は、現在のOpik公式Self-hosted手順を確認してセットアップしてください。

ただし、Opik本体の大量のDocker Compose定義を独自にコピーして保守しないでください。

可能な限り公式の起動方法を利用してください。

Application側からOpik SDKで二重に同じLLM Callを記録する必要はありません。

まずは、

```text
Agent Router
→ OpenTelemetry
→ Opik
```

を正規経路としてください。

---

# 19. Agent Router起動スクリプト

ローカル開発用に、

```text
scripts/start-agent-router.sh
```

などの起動手段を用意してください。

概念的には、

```bash
OPENAI_BASE_URL=http://127.0.0.1:2067/v1 \
OPENAI_API_KEY=sk-local-no-key-required \
OTEL_TRACES_EXPORTER=otlp \
OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf \
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://127.0.0.1:5173/api/v1/private/otel/v1/traces \
AI_GATEWAY_TRACING_SEMCONV=openinference \
aigw run
```

です。

実際には`.env`から読み込むなど、既存プロジェクトの方式に合わせてください。

URL末尾の、

```text
/v1
```

が二重にならないよう正規化してください。

---

# 20. OSについて

Agent Router 1.1のStandalone CLIである、

```text
aigw run
```

は現時点でLinux/macOS向けです。

実行環境がLinuxならそのまま使用してください。

Windowsネイティブ環境の場合は、

```text
WSL2
```

などの対応方法を公式仕様に沿って採用してください。

Windowsネイティブで動作保証されているかのような実装・説明をしないでください。

---

# 21. Health Check

以下を確認できる仕組みを作ってください。

## llama-server

少なくとも、

```text
GET http://127.0.0.1:2067/v1/models
```

などを使って生存確認してください。

## Agent Router

StandaloneモードではAdmin Endpointを確認してください。

想定:

```text
http://127.0.0.1:1064/health
```

Metrics:

```text
http://127.0.0.1:1064/metrics
```

## Opik

Opik UI/APIが起動していることを確認してください。

---

# 22. Smoke Test

最低限、次の自動テストまたは実行スクリプトを作成してください。

### Test 1: llama-server direct health

```text
llama-serverが生きている
```

### Test 2: Agent Router health

```text
Agent Routerが生きている
```

### Test 3: Agent Router → llama-server

Agent Router経由で、

```text
Hello
```

などを送り、正常な回答を得る。

### Test 4: DummyTaskRouter

```text
Auto
```

を入力し、

```text
qwen3.8-27b-exl3-3.5bpw-wm
```

が選ばれる。

### Test 5: Application → DummyTaskRouter → Agent Router → llama-server

完全なEnd-to-Endテストをする。

### Test 6: generation parameters

以下が壊れていないか検証する。

```text
temperature
top_p
top_k
min_p
enable_thinking
max_tokens
```

### Test 7: OpenTelemetry

Agent RouterからOpenTelemetry Traceが送信される。

### Test 8: Opik

Opik上でLLM呼び出しのTraceを確認する。

最低限、

```text
model
latency
token usage
input
output
```

のうち利用可能な情報が入っていることを確認する。

---

# 23. Unit Test

外部サーバーなしでも実行できるUnit Testを用意してください。

最低限、

```text
URL normalization
environment variable parsing
DummyTaskRouter
RouteDecision
Retry判定
Thinking parameter selection
Auto model resolution
```

をテストしてください。

ネットワークが必要なテストは、

```text
integration
e2e
```

としてUnit Testと分離してください。

---

# 24. `model="Auto"`を第一級の概念にする

Applicationコードからは最終的に、

```python
response = await client.complete(
    prompt,
    model="Auto",
)
```

と書けるようにしてください。

内部処理:

```text
Auto
 ↓
TaskRouter
 ↓
RouteDecision
 ↓
実モデル名
 ↓
Agent Router
```

Agent Router自身へ、

```text
model="Auto"
```

をそのまま送らないでください。

Autoを解決する責任はTask Routerです。

---

# 25. Provider abstraction

現段階ではProviderはlocalしかありません。

それでもコード内部に、

```python
if provider == "local":
    ...
```

のような条件分岐を大量に埋め込まないでください。

将来的には、

```text
local
openai
anthropic
gemini
bedrock
groq
```

などが追加されます。

基本的にProvider差分はAgent Routerへ押し込みます。

ApplicationはAgent RouterのOpenAI互換APIだけを見ます。

---

# 26. Agent Routerの責任

今回まだ利用しない機能もありますが、将来的に以下をAgent Routerへ持たせます。

```text
Provider abstraction
Authentication
Load balancing
Fallback
Retry
Rate limiting
Quota
Usage tracking
Model virtualization
MCP Gateway
```

Application側にこれらを独自実装しないでください。

---

# 27. Opikの責任

Opikは以下を担当します。

```text
Trace
Latency
Token usage
Cost
Evaluation
Experiment
Prompt analysis
Failure analysis
```

RoutingロジックそのものをOpikへ持たせないでください。

---

# 28. ログ

ログには少なくとも以下を出してください。

```text
request id
requested model
resolved model
router type
route reason
Agent Router endpoint
elapsed time
retry count
error type
```

ただし、

```text
API key
Authorization header
秘密情報
```

をログへ出してはいけません。

プロンプト全文についてもApplicationログには原則出さないでください。

詳細なLLM内容の観測はOpikへ寄せます。

---

# 29. Correlation ID

可能ならApplicationからAgent Routerへの呼び出しに、

```text
request id
session id
```

をHTTP Headerとして渡せるようにしてください。

Agent RouterのOpenTelemetry Header Mapping機能を使える場合は利用してください。

将来的に、

```text
Application Trace
↓
Agent Router Trace
↓
Opik
```

を追跡しやすくするためです。

ただし、初期実装を不必要に複雑にしないでください。

---

# 30. エラー設計

最低限、次を区別してください。

```text
TaskRoutingError
GatewayConnectionError
GatewayTimeoutError
GatewayUpstreamError
InvalidGatewayResponseError
```

既存コード構造に合わない場合は名前を変更して構いません。

ただし、

```python
raise RuntimeError(...)
```

だけですべてを潰さないでください。

元例外は、

```python
raise ... from exc
```

で保持してください。

---

# 31. 起動順序

READMEに以下を明記してください。

```text
1. llama-server
2. Opik
3. Agent Router
4. Application
```

llama-serverは今回すでに起動済みなので再構築不要です。

---

# 32. 開発用コマンド

可能なら以下のように簡単にしてください。

```bash
make check-llama
make opik-up
make agent-router-up
make smoke-test
make test
```

既存プロジェクトがMakeを使っていなければ、無理に導入せず既存方式に合わせてください。

---

# 33. ファイル構成

既存構成を優先しますが、概念的には以下を参考にしてください。

```text
src/
  llm/
    client.py
    config.py
    errors.py

    routing/
      base.py
      dummy.py
      models.py

infra/
  agent-router/
    README.md

  opik/
    README.md

scripts/
  start-agent-router.sh
  check-llama-server.sh
  smoke-test.sh

tests/
  unit/
  integration/

.env.example

docs/
  llm-gateway-architecture.md
  agent-router-opik-implementation-report.md
```

既存構造と衝突する場合は適切に変更してください。

---

# 34. 既存 `call_llama_server.py`

既存コードを捨てて全面書き換えするのではなく、まず依存箇所を調査してください。

現在の、

```text
call_llama_server()
LlamaServerEnvConfig
complete()
```

がどこから呼ばれているか確認してください。

既存利用箇所を壊さない形で、

```text
直接 llama-server 接続

↓

Agent Router経由
```

へ段階的に移行してください。

互換性が必要なら、

```python
LlamaServerEnvConfig
```

の外部APIを残しつつ、内部で新しいGateway Clientへ委譲してください。

---

# 35. Responses APIについて

Agent Router 1.1では、

```text
/v1/responses
```

も正式に対応しています。

そのため既存のResponses APIコードは削除しないでください。

ただし今回の初期E2E経路は、

```text
/v1/chat/completions
```

を優先します。

Responses APIについても追加のIntegration Testを作れるなら作ってください。

最終報告では、

```text
chat.completions: PASS / FAIL
responses: PASS / FAIL
```

を明示してください。

---

# 36. Agent Router設定を過剰に複雑化しない

今回はBackendが1個なので、

```text
custom Kubernetes CRD
InferencePool
複雑なFallback
複雑なLoad Balancing
MCP
Quota
Rate Limit
```

まで実装しないでください。

今回は将来拡張できる基礎だけ作ります。

最初はAgent Router standalone + environment auto configurationで十分なら、それを使ってください。

設定ファイルが必要になった場合のみ、

```text
config.yaml
```

を追加してください。

---

# 37. 本番化は今回の対象外

今回は、

```text
local development architecture
```

の完成を目的とします。

以下は対象外です。

```text
Kubernetes
高可用性
複数Agent Router
TLS終端
OAuth
複数Provider
本番Rate Limit
本番Quota
InferencePool
GPU Load Balancing
MCP Gateway
```

ただし設計上、後から追加可能にしてください。

---

# 38. README

READMEまたは専用ドキュメントに、最低限以下を書いてください。

```text
Architecture
Requirements
Installation
Environment variables
How to start Opik
How to start Agent Router
How to verify llama-server
How to run application
How to run tests
How to inspect Opik traces
Known limitations
Troubleshooting
```

特に、

```text
localhost
127.0.0.1
Docker内部
WSL内部
```

ではアドレスの意味が変わることがあるため、環境による違いを説明してください。

---

# 39. 完了条件

以下すべてを満たした場合のみ完了としてください。

```text
[ ] llama-server :2067 が生きている
[ ] Agent Router :1975 が起動する
[ ] Agent Router health check が成功する
[ ] DummyTaskRouter が動作する
[ ] model=Auto が実モデルへ解決される
[ ] Agent Router経由でQwenへ問い合わせできる
[ ] Pythonコードから問い合わせできる
[ ] Retryが過剰に重複していない
[ ] Thinking=false が動く
[ ] Thinking=true を検証している
[ ] top_k/min_pの通過可否を検証している
[ ] Opikが起動する
[ ] Agent RouterからOpikへOTel Traceが届く
[ ] OpikでTraceを確認できる
[ ] Unit Testが通る
[ ] Integration Testが通る
[ ] READMEが更新されている
[ ] .env.exampleが存在する
[ ] 秘密情報がコミットされていない
```

---

# 40. 最終報告

実装後は、

```text
docs/agent-router-opik-implementation-report.md
```

を作成してください。

以下を必ず記載してください。

## 実装したもの

変更ファイル一覧と役割。

## 最終Architecture

MermaidまたはASCII図。

## 動作確認結果

表形式で、

```text
llama-server health
Agent Router health
chat.completions
responses
Auto routing
Thinking false
Thinking true
top_k
min_p
OpenTelemetry
Opik trace
Unit tests
Integration tests
```

について、

```text
PASS
FAIL
NOT TESTED
```

を明記。

## Agent Router経由パラメータ互換性

特に、

```text
temperature
top_p
top_k
min_p
chat_template_kwargs.enable_thinking
max_tokens
```

について、

```text
Application
↓
Agent Router
↓
llama-server
```

で維持されたかを記録してください。

## 問題

発見した問題を隠さず記載。

## 未実装

今回は意図的に実装しなかったもの。

## 次の実装候補

将来的には、

1. 本物のTask Router
2. OpenAI追加
3. Anthropic追加
4. Gemini追加
5. Provider fallback
6. Cost-aware routing
7. Difficulty-aware routing
8. Model performance feedback from Opik
9. MCP Gateway

の順を候補として整理してください。

---

# 41. 実装時の判断原則

不明点がある場合は、推測でAgent Routerの設定を書かず、現在のAgent Router 1.1公式ドキュメントおよびOpik公式ドキュメントを確認してください。

特に、

```text
Agent Router
旧 Envoy AI Gateway
OpenTelemetry
Opik OTLP endpoint
Responses API
OpenAI-compatible backend
```

は2026年時点で更新が続いているため、古いブログ記事だけを根拠にしないでください。

既存コード・既存テスト・既存設計を先に読み、それらに合わせて実装してください。

実装途中で既存仕様と本指示が衝突した場合は、

1. データ破壊を避ける
2. 後方互換性を優先する
3. 変更理由を報告書へ記載する

の順で判断してください。

---

# 42. 最終目標

今回の完成形はこれです。

```text
                    ┌───────────────────┐
                    │    Application    │
                    └─────────┬─────────┘
                              │
                         model="Auto"
                              │
                              ▼
                    ┌───────────────────┐
                    │ Dummy Task Router │
                    │                   │
                    │ Auto → local Qwen │
                    └─────────┬─────────┘
                              │
                 resolved model name
                              │
                              ▼
                    ┌───────────────────┐
                    │   Agent Router    │
                    │   :1975 /v1      │
                    └─────────┬─────────┘
                              │
                OpenAI-compatible API
                              │
                              ▼
                    ┌───────────────────┐
                    │   llama-server    │
                    │      :2067        │
                    │                   │
                    │ Qwen3.8-27B       │
                    └───────────────────┘

                              │
                              │ OpenTelemetry
                              ▼

                    ┌───────────────────┐
                    │       Opik        │
                    │      :5173        │
                    │                   │
                    │ Trace             │
                    │ Eval              │
                    │ Token             │
                    │ Latency           │
                    │ Cost              │
                    └───────────────────┘
```

この構成を、まずローカル環境で再現可能かつテスト可能な状態まで完成させてください。